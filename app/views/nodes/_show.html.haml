= render :partial => "containers/list", locals: { containers: containers, node: node }
= render :partial => "volumes/list", locals: { node: node, volumes: volumes }

= render :partial => "nodes/chart"

= link_to t("common.remove"), node_path(node), data: { confirm: t("common.confirm") }, method: :delete, class: 'btn btn-danger btn-lg col-md-12'
