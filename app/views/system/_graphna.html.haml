= box_helper "success" do
  = simple_form_for Site.default, url: "" do |form|
    = box_header_helper t("system.graphna_config"), :cog do
      .box-tools.pull-right
        = form.submit t("common.update_btn"), class: danger_btn_helper

    = box_body_helper do
      = form.input :graphna_addr, label: t("system.graphna_addr")
